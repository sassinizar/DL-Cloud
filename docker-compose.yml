version: '3.3'
services:
  jenkins:
    image: jenkins/jenkins:lts
    ports:
      - "8080:8080"
    volumes:
      - jenkins_data:/var/jenkins_home
    networks:
      - ci-network
 
networks:
  ci-network:
 
volumes:
  jenkins_data:
